org.gradle.jvmargs=-Xmx3g -Dfile.encoding=UTF-8
android.useAndroidX=true
kotlin.code.style=official
# Termux 0.118.3 source refers to one AppCompat style through its module R class.
# Keep transitive R enabled on the isolated 4.0 branch so the upstream snapshot stays byte-for-byte.
android.nonTransitiveRClass=false
