# GitHub automation policy

GitHub Actions is intentionally disabled for this repository.

Do not add workflow files, request GitHub-hosted builds, or move compilation,
testing, signing, packaging or release publication into GitHub automation.
ChebyAgent artifacts are built and verified locally or on explicitly controlled
project hardware. GitHub stores reviewed source and version-bound Release assets.
